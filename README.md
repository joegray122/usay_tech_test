## USAY Technical Test Solution

To run tests using cypress UI: 

`npx cypress open` 

or headlessly with: 

`npx cypress run`

Note: webpack error with test for succesful login, wasn't able to resolve within the time! 